module.exports = function(gulp, options, plugins) {
    gulp.task('unit-tests', function() {
        return gulp.src('./tests', {read:false})
            .pipe(plugins.electronMocha({
                electronMocha: {
                    renderer: true
                }
            }));
    })  
};